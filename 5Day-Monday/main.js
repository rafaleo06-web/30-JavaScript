import { countries} from './countries.js'
import { webTechs} from './webTechs.js'

console.log(countries)
//console.log(webTechs)

//1-En la matriz de países, verifique si 'Etiopía' existe en la matriz; si existe, imprima 'ETIOPÍA'. Si no existe agregar a la lista de países. 

        //let index = countries.indexOf('Ethiopia')
        //if(index === -1){
            //countries.push('Ethiopia')
        //} else {
            //console.log('ETHIOPIA')
        //}
        //console.log(countries)\

//2-Encuentre el(los) país(es) intermedio(s)
        //var countrieMedios        
        //if (countries.length % 2 === 0) {
            //countrieMedios = countries.slice(countries.length / 2 - 1, countries.length / 2 + 1);
            //console.log(countrieMedios);
        //} else if (countries.length % 2 === 1) {
            //countrieMedios=countries.slice(countries.length / 2, countries.length / 2 + 1)
            //console.log(countrieMedios);
        //}  
//3-Divida la matriz de países en dos matrices iguales si es par. Si la matriz de países no es pareja, un país más para la primera mitad.
        //var tam=countries.length 
        //if (tam % 2 === 0) {
            //var primeraParte=countries.slice(0,tam/2)
            //var segundaParte=countries.slice(tam/2,tam)
        //} else if (tam % 2 === 1) {
            //var primeraParte=countries.slice(0,tam/2+1)
            //var segundaParte=countries.slice(tam/2+1,tam)
        //}
        //console.log(primeraParte);
        //console.log(segundaParte);